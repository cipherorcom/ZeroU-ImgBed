# ZeroU图床 Docker 部署环境配置
# =================================
# 复制此文件为 .env 用于 Docker 部署

# 环境配置
NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# 数据库配置
DATABASE_URL="file:./database/app.db"

# JWT配置（生产环境必须修改！）
JWT_SECRET=your_super_secret_jwt_key_here_please_change_this_in_production

# 存储配置
STORAGE_TYPE=local
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=10485760

# CDN配置（可选）
CDN_URL=

# 管理员配置（生产环境请修改）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# 图像处理配置
IMAGE_QUALITY=80

# 安全配置
ENABLE_RATE_LIMIT=true
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW=900000

# 功能配置
ENABLE_REGISTRATION=true
ENABLE_GUEST_UPLOAD=true

# 使用说明：
# 1. 复制此文件：cp docker.env.example .env
# 2. 编辑 .env 文件，修改JWT密钥和管理员密码
# 3. 运行部署：docker-compose up -d
